export enum variables {
  localHost = 'http://localhost',
  port = 8080,
  sqlite3 = 'sqlite3',
  urlUpload = 'uploads',
  tableNamePoint = 'point',
  tableNameItem = 'item',
  tableNamePointItem = 'pointItems',
}